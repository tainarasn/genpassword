// webpack.config.js

module.exports = {
    // other webpack configurations...
    externals: {
        react: "React", // Tells webpack to use React as an external dependency
        // Add other external dependencies as needed
    },
}
